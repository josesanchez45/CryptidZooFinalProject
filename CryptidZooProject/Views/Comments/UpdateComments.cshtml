@model Comments

<h2>Update @Model.DisplayName</h2>

@using (Html.BeginForm("UpdatecommentsectionDatabase", "Comment", "Post"))
{
    <input asp-for="CommentId" type="hidden" value="@Model.CommentId" />

    <label asp-for="DisplayName" class="control-label">Name</label>
    <input type="text" name="Name" class="form-control" value="@Model.DisplayName" />

    <label asp-for="Comment" class="control-label">Comment</label>    
    <input type="number" step="0.01" name="Comment" class="form-control" value="@Model.Comment" />

    <input type="submit" value="Update Comment" class="btn btn-primary" />
}